includes:
    - vendor/phpstan/phpstan-phpunit/extension.neon

parameters:
    level: 9
    paths:
        - src
        - tests
    tmpDir: var/phpstan
    reportUnmatchedIgnoredErrors: true
    ignoreErrors:
        # PHP cannot express an array's value type natively (e.g. list<string>)
        -
            identifier: missingType.iterableValue
        # PHP cannot express generic type parameters natively (e.g. IteratorAggregate<int, Event>)
        -
            identifier: missingType.generics
        # Amp async() resolves to mixed - PHP cannot express Future<T>, so awaited values lose their type at call sites
        - '#Parameter .+ expects .+, mixed given#'
        # Amp async() resolves to mixed - methods invoked on an awaited Future value cannot be type-checked
        - '#Cannot call method .+\(\) on mixed#'
        # Amp async() cannot bind its closure return to the Future template parameter - PHP cannot express Future<T> natively
        -
            identifier: argument.templateType
        # Test assertions that verify runtime behaviour are valid even when PHPStan can infer the result statically
        -
            identifier: method.alreadyNarrowedType
            path: tests/
